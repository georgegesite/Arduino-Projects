#include <PCM.h>
const unsigned char sample [] PROGMEM = {

};

void setup() {
  // put yourstart setup code here, to run once:
  startPlayback(sample, sizeof(sample));
}

void loop() {
  // put your main code here, to run repeatedly:

}
